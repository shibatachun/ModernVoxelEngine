#pragma once
#include "GlmUtils.h"
namespace utils {
	namespace math {
		glm::vec3 VecMin(const glm::vec3& a, const glm::vec3& b);
			

		glm::vec3 VecMax(const glm::vec3& a, const glm::vec3& b);
	}
}